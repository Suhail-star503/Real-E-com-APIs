export default class ordermodel {
    constructor(userid, totallamount, timestamp) {
        this.userid = userid;
        this.totallamount = totallamount;
        this.timestamp = timestamp;
    }
}